services:
  handoff:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    environment:
      - SESSION_SECRET=${SESSION_SECRET:-change-me}
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-http://localhost:80}